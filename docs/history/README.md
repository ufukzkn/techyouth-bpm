# Documentation History

This directory preserves chronological implementation records that are useful
for archaeology but should not be treated as the current product contract.

- Current decisions and agent rules live in `../01-agent-notes.md`.
- Current architecture, API, security and test contracts are linked from
  `../README.md`.
- Historical entries are retained in full so no implementation context is lost
  while active documents stay concise and non-duplicative.
- `09-ozgun-form-flow-implementation-log.md` preserves the form UX and validation
  iteration record; the active contract remains `../09-ozgun-form-flow.md`.
